# Test data (import-test branch only)

Upload your extracted book files here to test the importer:
JSON files, their image files, or a ZIP of the whole book folder.

This folder lives only on the `import-test` branch so book content never
lands on `main`.
